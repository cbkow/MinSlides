{
  "name": "slide-deck",
  "version": "1.0.0",
  "private": true,
  "description": "Password-protected slideshow with scroll-snap and video.js",
  "scripts": {
    "build": "node build.js",
    "build:secure": "node secure_build.js"
  },
  "devDependencies": {
    "staticrypt": "^4.0.0"
  }
}
